# Production Docker environment for go-invoices

# Database (connects to postgres service)
DB_HOST=postgres
DB_PORT=5432
DB_USER=invoices
DB_PASSWORD=invoices123
DB_NAME=invoices
DB_SSLMODE=disable

# Server
PORT=8080
SERVER_READ_TIMEOUT=15
SERVER_WRITE_TIMEOUT=15
SERVER_IDLE_TIMEOUT=60

# Application
DEV=0
MIGRATIONS=1

# Postgres container credentials
POSTGRES_USER=invoices
POSTGRES_PASSWORD=invoices123
POSTGRES_DB=invoices
